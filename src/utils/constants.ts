import path from 'path';

export const BLOG_DOCS_DIR = path.resolve(process.cwd(), 'contents/blogs');

export const PROJECT_DOCS_DIR = path.resolve(
  process.cwd(),
  'contents/projects'
);

export const JSON_File_DIR = path.resolve(process.cwd(), 'src/data');
